# SEN-014: exact, pinned build dependencies for the Windows agent exe.
# Unpinned `pip install yara-python pyinstaller` pulls whatever is latest at build
# time (typosquat / backdoored-release / surprise-transitive risk) into a SYSTEM-run
# binary. These are the exact versions the build is validated against; bump them
# deliberately, never implicitly. (CI hardening residual: add --require-hashes with a
# --generate-hashes lockfile from an internal mirror; see docs/project/SECURITY.md.)
pyinstaller==6.21.0
pyinstaller-hooks-contrib==2026.6
yara-python==4.5.4
altgraph==0.17.5
packaging==26.3
pefile==2024.8.26
pywin32-ctypes==0.2.3
setuptools==83.0.0
